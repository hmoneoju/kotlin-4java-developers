package week1

import week1.Color.BLUE
import week1.Color.ORANGE

fun main(args: Array<String>) {
    // val name = if (args.isNotEmpty()) args[0] else "Kotlin"
    // print("Hello $name!")

    // listToStr
    //val result = listToStr(listOf("a", "b", "c"))
    //print("$result")

    // displaySeparator
    // runDisplaySeparator()

    // print(getDescription(BLUE))
    //print(mix(BLUE, ORANGE))
    //print(mix(BLUE, BLUE))

    for (ch in "abc") {
        print(ch + 1)
    }}